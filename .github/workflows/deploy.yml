name: Deploy to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fumadocs bootstrap is validating the new site before Pages deployment is re-enabled."
